// SPDX-License-Identifier: UNLICENSED
pragma solidity ^0.8.13;

import { LogNormalSetUp, LogNormal } from "./SetUp.sol";

contract LogNormalConstructorTest is LogNormalSetUp {
    // function test_LogNormal_constructor() public {
    //     LogNormal logNormal = new LogNormal(address(dfmm));
    //     assertEq(logNormal.dfmm(), address(dfmm));
    // }
}
